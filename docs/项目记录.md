# 项目记录

## 目的和已确认范围

建立独立的个人技能项目，展示技能用途、安装和使用方法，持续发布更新，并接受别人提交PR。维护者为 naiman-debug。

2026-09-13：用户确认采用前轮建议并要求在当前目录复制和处理技能。已采用：名称naiman-skills；单仓库多技能；MIT；中文完整及精简英文说明；首版面向Codex；固定Release手动安装更新；整仓统一版本；两个主入口与两个辅助技能。不把个人用户级目录整体上传，不带入私人项目材料。

此前授权本地复制、通用化、说明和验证。2026-09-13用户进一步明确：条件满足即可推上线，已授权通过发布检查后的推送；范围仍为三个主技能及两个必要辅助目录。用户现有安装技能保持不动。公开上传、远程仓库创建及发布尚未执行，执行事实见下方当前返回点。

## 文件职责和来源

2026-09-13 最新范围：本地维护工作区有七个顶层技能目录；用户指定本次测试与拟上线的主技能为项目建档、项目收工、步步高，加上两个收工辅助共五个公开目录。reader-first-writing与新建的skill-release-prep保留在本地维护工作区，不纳入本次五目录公开候选。下文四技能、五技能、六技能阶段均为历史快照。当前没有正式Release。

项目入口为根README；行为和读取规则见根AGENTS；维护流程见CONTRIBUTING；安装步骤见安装与更新。按最新用户确认，README展示项目建档、项目收工与步步高三个入口，完整配套安装组合见安装与更新，不另设第二张易漂移清单。

四个技能来自维护者提供的现有技能，经本轮制作公开副本。项目建档原有的内容引擎与健身案例为教学材料；参考依据在其 references/sources.md。没有复制私有项目原件。许可证署名使用公开GitHub账号naiman-debug；若后续加入第三方原文、代码或资产，应分别确认许可并保留声明。

## 首轮四技能建设快照

候选版本0.1.0的本地建设已完成：四个完整公开技能目录、中文与英文入口、单技能说明、安装更新教程、贡献发布流程、MIT许可证以及无第三方包依赖的静态检查工具。本地Git已初始化为main分支，尚无提交、远程或版本标签；没有公开上传。

公开版已去掉固定模型与思考强度、强制任务卡和个人文档元数据要求；保留目录职责、重要材料索引、来源状态和维护责任这些建档结果。健身案例与模板按当前入口方式整理。

收工三件套明确配套关系和缺依赖处理，直接维护既有责任文件，不再转交个人状态同步技能。缺AGENTS或索引时先按README与当前请求限定范围，不自动另建治理体系。完整收工、只准备、只检查分开处理；独立明确提交请求可使用本次准备结果。已有删除或改名须另有授权及恢复核查，技能不执行新的删除或搬移。

## 首轮验证与限制

2026-09-13完成以下检查：

| 检查 | 实际结果 | 证明范围 |
| --- | --- | --- |
| 四技能基础格式 | 全部通过 | 入口字段和基础格式 |
| 仓库静态检查 | 4个技能通过 | 版本、声明依赖、本地链接、界面字段及明确路径/模型模式 |
| 检查器自身测试 | 7/7通过 | 完整包、HTTPS引用、缺依赖、版本错配、断链、越界链接、作者绝对路径等样例 |
| 新项目建档 | 生成README、AGENTS及一份合并索引/目录职责；原始输入保留 | 实际产物能定位重要材料、来源、维护者和维护时机；无占位符、断链或无关功能目录 |
| 收工audit | 文件、HEAD、暂存区均未变化 | 该只读样例保持只读 |
| 收工readiness_only | 形成准备结果，文件和Git均未变化，未另造记录 | 该样例正确排除他人改动，未暂存或提交 |
| 正常full | 练习提交bc9b46e，仅含notes/result.md；他人改动保留 | 实际本地提交符合指定文件范围，未推送 |
| 未知暂存项 | 保持同一HEAD，未知暂存未清空或夹带，任务修改未提交 | 该样例正确停止受影响提交 |
| 缺Git辅助技能 | 报告缺失，完成可执行准备，未替代、暂存或提交 | 不把缺依赖的full宣称为完整完成 |

行为验证由独立检查者安排受控执行者，使用系统临时目录里的公开包副本及虚构材料；缺依赖场景另用没有读取过完整包的执行者和干净练习仓库。主线回读了建档产物及Git实际提交/残余状态。这是受控执行证据，不是新设备、全新账号或真实用户自动发现技能的证明。

仍未验证：新用户环境的自动发现、安装后实际加载、完整升级/回退；正式Release重新下载；其他客户端或操作系统；已有规则复用、已授权删除/改名存档等未包含在本次样例中的分支。格式通过也不等于完整隐私或来源许可审计。首版是否可公开发布仍应依据贡献指南中的相关前置检查。

本轮测试现场保留在系统临时目录，未进入仓库。两个Python缓存目录的清理命令被执行策略阻止，已被.gitignore排除；未改用其他方式删除，也不把它们纳入发布文件。

## 下一步与返回点

### 首发目标已创建，正在准备上线（2026-09-13，当前）

用户随后明确要求操作已打开的内置浏览器。已在登录的naiman-debug账号下创建公开空仓库naiman-debug/naiman-skills，没有初始化README、许可证或其他远程文件。连接器随后实际读取仓库成功，返回push与admin权限为true，规则集为空；单独的协作者权限接口仍受集成权限限制，不能把该接口403误写为全部仓库无写权限。

当前继续按已授权范围发布三个主技能及两个辅助目录：更新真实下载入口，冻结准确文件，由独立审核者复核本轮变化后上传并创建v0.1.0预发布。旧技能行为实测不重跑；实际发布后重新下载验收仍需执行，不能提前宣布。两个未选技能、测试目录、凭据和用户级安装均不进入公开物。

### 此前目标访问阻塞（2026-09-13，已由浏览器创建仓库解除）

用户明确允许符合条件后推上线，不再以缺少推送意图授权为阻塞。重新查询目标naiman-debug/naiman-skills仍返回404，连接器权限结果为空；不能确认目标是否存在或有写入权限。GitHub连接器没有创建仓库工具，浏览器两次因请求头策略加载错误无法打开创建入口。本机Git凭据管理器及Git当前配置的凭据通道均收到GitHub 401认证拒绝，未输出或写入源码任何凭据。设备登录进程没有返回可操作地址或验证码，核对具名进程后已停止，终端确认结束；没有重复扩大权限或关闭证书验证。

当时的返回点是创建目标空公开仓库并恢复连接访问；该项现已按上方最新记录解除。此前五包实测及独立审核保留，下面保留当时证据，不作为当前远程状态。

### 完整发布准备流程与三技能实测（2026-09-13，当前任务）

用户指出上一轮仅完成局部审核，不能把它当作整个发布准备；本轮已明确要求整体设计可重复执行的最终流程，以三个真实主技能完整测试，并要求实际测试后、上线前各有独立Agent复核。新增本地维护技能skill-release-prep，具体门槛与仓库操作约定分别由技能和CONTRIBUTING承担，AGENTS只保留入口和职责。较轻的包装、措辞复核可以采用较轻模型或中档思考；涉及写入、Git、依赖与多轮决策时选能承担相应判断的模型，不把某个型号写成公开技能的强制依赖。本轮主要执行为Terra，独立审核沿用前轮Sol。

完成标准改为：选定范围及传递依赖全部处理，公开内容与许可核对，真实发现和调用、关键正反行为、更新和回退、准确发布内容及目标权限、两次独立复核全部满足，且没有关键待定，才能报告“发布准备完成”。真正受阻时继续不依赖该门槛的工作，最终如实报告阻塞；不以本地文件齐全替代整个结果。公开化阶段在行为测试后的独立审核可计作第一次复核，不额外叠成三次；候选内容变化只重验受影响部分。上传、Release及发布后重新下载验收属于后续真实外部动作，不能在准备阶段提前声称完成。

本次使用真实所选五个技能包，在系统临时目录naiman-release-prep-20260913中测试，既有本机安装副本没有修改。当前已有的实际证据：

| 项目 | 实际结果与边界 |
| --- | --- |
| 干净宿主发现 | Codex 0.154.0-alpha.6.2 app-server，隔离用户主目录和Codex目录，实际发现候选安装路径。初次多发现步步高内三个原版入口；修订后只有五个候选根入口及系统自带技能，无发现错误。 |
| 步步高包装修复 | 将三份上游SKILL.md改名为UPSTREAM-SKILL.md并更新引用；与修订前副本逐字节比较，上游原文未改。新增检查器反例防止再次混入嵌套入口。 |
| 独立源码复核 | 处理Git辅助反向requires与readiness输入契约的混淆、步步高无委派能力时的事实查证退路，以及三个收工README安装后断链；上游原文和许可保留。 |
| 更新和回退文件层 | 用真实五包加明确标为测试的本地修改演练；更新后相对路径和内容与候选一致，旧额外文件不残留，本地修改完整保留并可整套回退。首发没有旧Release，此项不是两个正式版本之间的升级证明。回退后的五包已被干净宿主发现。 |
| 新维护技能 | 基础格式与干净宿主发现通过；它不属于此次五目录读者包。 |
| 独立CLI调用 | 初次连接证书失败；后续进程已启动模型，但文件读取命令被策略阻止，未完成候选加载和行为调用，不计通过。扩大为自动审批工作区沙箱的请求被自动审核拒绝，已停止此方法，没有关闭证书校验或绕过拒绝。 |
| 远程目标 | 当前GitHub连接可读取维护者账号；目标naiman-debug/naiman-skills查询404，权限查询为空。不能据此区分不存在与未获访问，更不能声称有发布权限。本轮未创建远程、上传或发布。 |

受控Agent已在候选安装副本上执行建档、收工和步步高实际案例。建档只读空目录保持为空，已有项目复用原记录；空目录正向生成仅创建获准的README和AGENTS，链接可解析，无占位符或额外目录。收工覆盖audit、readiness_only、full、未知暂存和缺辅助技能：full实际提交仅包含本任务记录与结果文件，无关修改保留；未知暂存项没有被清空或夹带，缺辅助技能没有从本机补读。独立审核者直接回读实际产物和Git状态，已确认既有项目及收工案例的可观察结果；新增空目录正向产物一并提交最终复核。

步步高三轮讨论选用地图指向的当前Context、保留历史根Context，更新现有Road并在暂停时保留未决事项。测试发现第二轮推荐用精简附件代替完整公开标签，违背上一轮已确认范围。修订仅在Road增加每次推荐前核对已确认边界、取舍和Context的要求，未修改上游原文或增加第三项能力。另一位独立Agent读取修订候选重新讨论该分支，明确否定附件可替代标签范围，独立审核者据此关闭这一具体行为问题；原失败保留，不扩大成所有多轮讨论稳定通过。

宿主发现、受控Agent执行、独立CLI实际调用是不同证据层；前两者不得自动改写为第三项通过。源码静态检查覆盖七个本地技能并通过，检查器测试8/8通过；五目录候选及其实际解压包也通过静态检查。这些都不解除真实调用和远程门槛的阻塞。

本地复核后记：独立审核者已补验空目录正向产物，以及缺AGENTS、索引和Git时仅保留既有任务文件、未擅自建档或初始化Git的保护案例。第一次受控行为产物复核完成；结论对应实际产物及可见Git状态，不代替独立宿主调用证据。

第二次最终内容复核针对独立临时仓库的Git tree `d04d7b694274175d0934ae2c2ce47bf26aa22e18`、其导出包naiman-skills-0.1.0-reviewed-tree.zip及实际解压目录完成。准确包含58个文件、五个技能目录；两个未选技能的源码不在其中。版本、依赖、许可、上游原文、相对导航及完整安装更新说明通过该范围复核。导出包有五个普通文本文件按Git设置转换换行，归一化后内容相同，不宣称全部字节一致。该固定快照保留了复核前的项目记录；本段后记属于本地维护记录，不冒称已进入原冻结包。后续形成正式候选时，只针对实际变化重新核对。

此阶段曾因远程目标受阻；下方桌面补验已解决准确候选路径无法实际读取和运行的问题，独立CLI失败保留为未覆盖环境，不再当作没有任何真实调用证据。最新远程状态与返回点以上方首发记录为准。临时树与附件不等于正式公开标签，需要在获准的目标引用上完成实际发布，不能把旧快照审核扩大为新内容已经发布。

### 当前目录内的桌面新任务补验（2026-09-13）

用户明确要求在当前目录建立独立子目录，已使用Git忽略的 `.test-output/desktop-skill-test/`；这项授权不包含创建远程仓库或上传。五个完整安装目录来自前述已审阅包，既有用户安装保持不动。独立桌面任务 `01a09ab1-3218-71d1-94d9-384d16cbf5de` 实际按测试安装路径读取五个入口及所需随包参考，没有沿用本机同名技能。

项目建档只生成并回读README和AGENTS；项目收工在独立练习Git仓库形成提交 `870034ff13e0d774ada16c62f9deaa8345e4fe2e`，仅含README、TASK和notes/result，暂存区为空，无关草稿修改保留且无remote；步步高完成两轮受控讨论，复用已确认范围、保留未决问题并暂停，没有创建产品文件。测试者答案明确标为受控试用材料，不代表真实产品需求。独立审核者直接读取新任务工具输出、安装副本和实际文件/Git状态，确认这三项显式调用结果。

证明范围是**真实Codex桌面新任务的显式路径加载与调用**，不是父项目自动递归发现子目录里的技能。本次新位置app-server初始化超时；此前干净发现只覆盖原consumer位置，不能移作新位置成功证据。独立CLI、跨机器、新账号和正式Release重新下载仍未验证。贡献指南允许真实客户端新会话，不要求CLI为唯一入口；因此不再为这三项显式调用重复运行失败CLI。现场RESULT.md及练习目录保留在上述Git忽略路径，公开包不包含测试现场。

### 将实测经验纳入发布准备（2026-09-13）

用户要求把独立子目录试用和此前发现的问题纳入可重复执行的整体流程。继续采用一个skill-release-prep：入口推进全流程，release-preparation维护完成门槛，新增testing-lessons参考维护隔离目录、加载证据、分轮测试和问题修复方法；这些职责服务同一发布准备结果，不新增技能包或运行脚本。

已纳入实际经验：候选完整复制与发布排除、项目规则不混入消费者项目、桌面新任务与CLI的不同证据范围、环境失败的有界定位、上游嵌套入口误发现、安装后说明断链、readiness输入与安装依赖混淆、建档模板过强、步步高推荐偏离既有取舍，以及Git保护和更新回退案例。保留原失败和受影响复测范围，不将个别案例强制为每个技能的全部测试矩阵。具体现场留原记录和忽略目录，可复用指导写在维护技能参考中。

同步技能README、AGENTS与贡献指南；独立审核发现并修正了将一次子目录授权写成长效规则，以及无依赖技能也被要求做缺依赖测试两处问题，定向回读后通过规则增量复核。仓库静态检查覆盖七个技能通过，维护技能基础格式通过。本轮复用前述真实案例证据，没有重跑全部业务案例，也没有改变已安装技能或执行远程发布。维护技能继续不属于首发五目录包；公开说明后续变化须在正式冻结时核对，不能沿用旧快照宣称新内容已被最终发布审核。

### 项目建档的公开化审核与真实项目试用

用户要求开始实际发布准备，并由独立Agent结合真实案例审核，重点检查项目建档是否残留个人项目绑定。本轮只修改仓库公开副本和获准的临时测试副本，不修改已安装技能，不扫描或复制GCM等其他私人项目，不公开上传。

独立审核者完整阅读项目建档的22个文件，未在本次范围内发现可识别账号、凭据、作者绝对路径、私人项目正文或必需但未随包提供的外部技能。内容引擎和健身案例保留为教学材料，不能作为实际执行证据；此结论不等于全仓保密性审计或来源权利的全面证明，参考外链本轮未重新核对。

实际修正：入口补充模板选用条件，避免同时生成两套规范或重复索引；内容批次、项目规范、当前规范、生成与维护及扩展记录取消不适用项目的固定ID、revision、批次目录、逐文件登记和重复批准要求；空目录可从用户目的开始；单技能README删除仅复制技能后失效的仓库导航链接，并说明完整目录安装更新。保留来源追溯、原始材料和当前/历史关系的核心要求。

维护流程已写入CONTRIBUTING的“把自用技能整理为公开候选”，AGENTS只保留触发和职责要求。先完成真实试用与问题修复，再决定是否提炼独立发布技能，当前不新增一套技能或重复流程文档。

真实案例使用本仓库实际文件的隔离副本，由另一位执行者读取候选包并运行建档流程：先只读盘点，再按授权在原有项目记录追加当前状态。只读阶段60个文件无新增、删除或内容修改；补齐测试准备时漏复制的tests后，写入阶段62个文件中仅docs/项目记录.md变化，无新增、删除，不创建重复规范/索引，不改首页范围，也未操作Git。该副本不含.git是隔离安排；漏复制tests由主线补齐，不能记为源技能缺陷或通过删除测试承诺来掩盖。

主线回读实际结果：执行者保留历史快照，说明六个目录、三个入口与五目录安装组合的区别，保留reader-first-writing的待确认发布范围。此次只证明真实已有规则项目上的只读和最小记录写入，没有覆盖全新用户自动发现、安装加载、所有模板生成或其他业务领域。修订后的仓库静态检查通过（六技能），检查器自身测试7/7通过。

独立审核者随后直接比较临时副本与基线并从AGENTS、README导航至实际追加记录，确认该案例没有越界、重复建档或选错责任记录，未改变首页与发布范围。它未看到执行者完整对话，因此只确认实际产物与可见差异，不证明指定技能实际加载或判断过程稳定。测试现场保留在系统临时目录naiman-startup-release-7ff4c760，未纳入源码。返回点：继续验证独立安装加载及更新/回退，确定首版准确公开范围；本轮不宣布全仓可发布。

### bubugao 两项补充版

在恢复原版后，用户明确要求执行收紧方案：以原版为基础，仅增加Road讨论路线和Context适用性检查，使用独立英文名bubugao、中文显示名“步步高”。已创建 [bubugao说明](../skills/bubugao/README.md) 并安装独立目录，原版grill-with-docs保留，调用时用英文名区分。原版恢复记录是此前已完成的步骤，不再代表只允许使用原版的最新决定。

随包保留原版入口、grilling、domain-modeling及CONTEXT/ADR格式文件，来自上游提交3cca18b368ae95cdbdebbff572ccafa662551015；逐文件与已下载上游内容比对一致，保留Matt Pocock的MIT许可。两项补充仅在新入口中，不要求额外安装依赖，不恢复此前三模板或完整需求真值方案。

Road保存当前范围内必要进度和未决问题，不保证普通需求全部落盘。Context检查先判断实际文档的项目/领域、有效性、职责和冲突，再选择沿用、局部修订、历史参考或待确认。主线回读核对只读、不适用旧Context、多领域归属和暂停边界；尚未进行独立多轮行为试用。基础格式、六技能仓库检查及安装副本比对通过。支撑面检查对上游CONTEXT-FORMAT代码块内三处虚构路径报缺失，并对原版英文提示语言警告；人工核对为示例路径，不建立假文件或修改原文来消除提示。

用户随后明确要求将步步高纳入准备上线的技能，并解释原版来源及增强能力。已同步中英文首页、单技能说明及安装组合：署名Matt Pocock，说明基于grill-with-docs修改，仅增加Road与Context适用性检查，保留上游MIT许可。此前首页仅两个入口的限制由本次用户要求更新为三个入口。仓库仍有六个技能目录，首页三个入口所需组合共五个目录；reader-first-writing继续作为单独候选。

本次更新为公开说明和导航，未改技能执行规则或同步本机安装副本，未公开发布。下一步可在新会话使用$bubugao实际试用Road更新和Context选择，不把当前文件检查当成真实调用验收。

### 原版 grill-with-docs 已恢复

用户撤回扩展方案，明确要求删除仓库扩展和本机已修改的旧版，不备份，重新使用上游原版。Agent的删除命令被执行策略拒绝后，用户自行执行删除；本轮核实两个旧目录均不存在，仓库现恢复为五个技能，不再包含旧扩展。

本机已安装 mattpocock/skills 提交 `3cca18b368ae95cdbdebbff572ccafa662551015` 的 grill-with-docs，并将 grilling、domain-modeling 两个依赖同步为同一提交的上游文件。逐文件比对通过；执行入口及模板未改，保留上游显式调用策略。不加入Road、Context适用性判断或此前三模板扩展，不修改其他技能的规则。旧内容未备份，原版不复制进公开仓库，未发布。

2026-09-13：用户将原版 grill-with-docs 的中文界面显示名确定为“边聊边记”，已更新本机 `agents/openai.yaml` 的 `display_name`。只改显示名，原版执行入口、说明、依赖规则与显式调用策略保持不变；带 Road 和 Context 检查的 bubugao 仍显示为“步步高”。

已核对安装文件及仓库静态检查；真实新会话调用与文档生成尚未验证。本会话可能仍保留旧指令上下文，实际试用应在新会话显式调用 $grill-with-docs。

### CCD-CC 简介

根据用户请求新增 [CCD-CC技能包介绍](CCD-CC技能包介绍.md)，介绍任务门禁、受控代码执行、按需复核和协作链的用途及示例。已读取本机四个对应技能的执行入口；当前仓库未发现该技能包，文档明确区分本机已有技能与仓库待收录状态。本次仅编写介绍，没有复制技能、调用协作流程或验证外部通道。后续若要收录，再核对完整依赖和公开使用条件。

### README表达调整

用户要求对外说明从自己的实际痛点和使用场景出发：先说明这是Naiman的个人技能库，再各用五六句话解释为什么做项目建档、为什么需要打完收工；展示列表只保留两个主技能及链接，去掉前部的版本/验证汇报和功能安装表。本轮由Gemini起草，主线做少量措辞和排版整理，原稿保留在系统临时目录。

中文README已按此调整。原有“先试一次”、下载更新、维护与贡献部分原样保留，按用户要求以后再改；英文README本轮未同步，后续对外版本统一时处理。

### 面向读者写作候选

用户随后确认将这种写作方式独立成小技能，并要求一次完成设计、审核、生成和测试。本地新增“面向读者写作”，目录名为 reader-first-writing，版本仍为未发布的0.1.0；其源码保留在本地维护工作区，不随本次五目录候选公开。它主要改写对外短文档，也可根据真实材料起草；按读者目的组织内容，不强套痛点模板，不编造经历或效果，不绑定模型。与单纯校对或去AI腔相比，重点在于选择读者需要的信息及呈现顺序。

仅新增执行入口、简短中英文说明和界面配置三个文件，无脚本、模板库或其他技能依赖。此前四技能建设和验证记录保留其原始覆盖范围；仓库现有五个技能。按用户先前指定的首页展示范围，暂不更改两个主技能链接，候选在本记录提供入口；正式纳入展示时再同步中英文首页。没有安装到用户级目录，也没有公开上传。

新技能基础格式与支撑文件检查通过，仓库静态检查覆盖五个技能并通过，检查器自身测试7/7通过。这些结果只证明对应格式和规则检查，不证明安装后加载或写作质量。

独立审核者读取候选技能并完成三个虚构材料写作样例：图片整理工具的内部进度稿改为用途说明，保留预览与无拍摄日期不处理的限制；只纠错请求仅修改错字；CSV工具简介未采纳无依据的“每天节省两小时”，并说明所缺依据。主线回读了实际输出，未发现这些样例中的事实新增或范围扩大；采纳一处触发描述澄清，区分“不是主要校对用途”和“显式只校对时遵守请求”。这些是受控文本试用，不证明不同模型的稳定表现或真实安装加载。候选设计、文件生成、独立审核及上述样例检查已完成，尚待用户实际试用反馈。

本轮复制与公开副本处理已完成，可从README审阅和在练习环境试用。下一步是在独立用户环境走通安装、加载、升级与回退，核对首次发布需要的剩余行为分支，再确认准确公开文件与目标GitHub仓库权限，按授权创建远程、提交和发布。当前没有正式Release，不能把候选版本号、账号读取成功或本地测试通过当成已经发布。
