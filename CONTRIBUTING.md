# 参与贡献与维护

欢迎改进已有技能，也欢迎增加确实解决一个问题的新技能。第一次可从修正文案或补充一个真实使用例子开始。公开仓库为 [naiman-debug/naiman-skills](https://github.com/naiman-debug/naiman-skills)；本地修改仍须按本指南完成检查、范围核对和相应授权后再进入远程动作。

## 新增一个技能

1. 说明使用者遇到什么问题、会怎样发起请求，以及完成后能看到什么结果。先确认已有技能不能直接解决它。
2. 在 `skills/` 新建一个小写英文加连字符的目录，目录名与 SKILL.md 的 name 一致。
3. 写 SKILL.md 的触发条件、主要流程、授权边界和必要参考。metadata.version 使用根 VERSION 的值；需要其他技能时以逗号分隔字符串写 metadata.requires，并在使用说明解释组合。
4. 增加简短 README：用途、前提、依赖、调用例子、可能修改什么、不支持什么。增加与行为一致的 agents/openai.yaml。只有实际需要时才增加脚本、模板和参考。
5. 只有成为对外用户入口时才更新中英文仓库首页的技能列表。维护工具或候选技能先在相应维护记录说明身份，不因新建目录擅自加入首页或读者安装组合。使用虚构示例；确认所有内容都可按本仓库许可证分发。
6. 完成下方验证后再提交。不要为了新增技能顺便重构全部技能。

metadata.version 和 metadata.requires 是本仓库的字符串字段；后者用于静态依赖检查，不会让Codex自动安装其他技能。

## 修改已有技能

先找到一个具体问题或可复现的失败，再做最小修改。SKILL.md、参考、模板、界面提示和人读说明需要一起检查；只改入口可能留下相反的旧规则。

公开源文件在本仓库维护，不直接改维护者机器上的已安装副本。同名已安装版本不构成依赖已经发布的证明。不要提交真实账号路径、模型密钥、私人项目资料、聊天记录或测试运行现场。

## 把自用技能整理为公开候选

1. 选定一个技能及其完整依赖，核对入口、参考、模板、脚本和界面提示；只复制这些具名文件，保留来源与适用许可。
2. 阅读内容，找出作者路径、私人正文、账号配置、固定模型、未随包提供的依赖，以及只适合原项目的强制规则。私人内容移出公开候选；通用能力改为可配置或按需采用。教学案例与私人原始材料要分别判断，不用关键词扫描代替内容审核。
3. 用一个明确请求实际执行候选包。已有可用真实项目时，使用获准材料的隔离副本；没有合适材料再用自制案例，并如实标注。测试者只读取候选包，不从本机已安装技能补依赖。先验证只读，再在授权范围做实际写入，检查前后文件变化。
4. 首次公开或涉及写入边界、模板选择、依赖的实质变化，交由未参与修改的独立 Agent 审核源文件和实际产物。它应核对入口能否找到内容、是否重复建档、是否遗漏来源和是否越界，不能只评价执行者的汇报。如果这次审核发生在行为测试后、对象和证据与发布候选相同，它就是发布准备的第一次复核，不额外再做一遍；冻结后仍需第二次最终复核。普通小修按影响范围回读即可。
5. 修复发现的问题，重跑受影响场景；在现有项目记录保存具体结果、未覆盖部分和返回点。首次公开发行或版本更新还要按下方“本仓库的发布准备”完成完整门槛。整理公开副本、提交代码、发布 Release、更新本机安装是不同动作，各自按已授权范围执行。

独立 Agent 的结论仍只覆盖它实际检查的案例；未经新用户安装加载验证，不能宣称已经证明所有用户可独立使用。真实测试材料只留在获准的测试目录，公开贡献记录用不含私人信息的结果摘要。

## 验证方法

在仓库根目录运行，需要Python 3.10或更新版本，无第三方Python包：

```text
python scripts/validate.py
python -m unittest discover -s tests -v
```

检查器核对本仓库约定的入口字段、版本、声明依赖、Markdown相对链接、基本界面字段和少数明确的不可移植内容。它不实现完整YAML解析、不验证外链可用性，也不能证明没有所有类型的私人信息。模板中的生成目标路径按模板用途处理，实际生成文件仍需行为测试。

检查器测试是给检查器自身用的；没有证明技能已正确执行。公开发布前还应在独立练习目录，用**仅来自发布包**的技能进行以下调用，记录实际文件和Git结果：

| 场景 | 观察结果 |
| --- | --- |
| 空项目建档 | 生成可读入口和必要规则，占位符已替换，不生成无需要的业务目录 |
| 已有项目规则 | 复用已有入口和目录，不另外生成相互冲突的规范 |
| 建档只读请求 | 只给建议，不产生文件 |
| 收工audit | 文件、HEAD和暂存区不变 |
| 收工readiness_only | 可以更新获准的责任记录，但不暂存或提交 |
| 完整收工 | 只提交本任务明确文件；保留无关改动、不推送 |
| 未知暂存项 | 受影响批次停止，不清空暂存区、不夹带他人文件 |
| 缺AGENTS、索引或Git | 明确实际覆盖和缺失；不自动建档或初始化Git |
| 缺配套技能 | 明确缺失及受影响功能，不去维护者的安装目录补读 |
| 安装、升级、回退 | 核对实际加载路径、整套版本及至少一次实际调用 |

普通文案变化只重查影响的项目。修改写入或Git边界时做对应正向和拒绝场景；修改安装器或校验器时做故意损坏的样例。测试放系统临时目录，使用虚构身份和材料，不推送远程。不要把每次检查都变成全仓审计。

## 本仓库的发布准备

`skill-release-prep` 负责通用推进、冻结和准备状态判断；本节只给出本仓库当前的发行组合、命令和发布约定。它不替代下方的实际发布动作，也不把本地检查等同于已发布。

测试目录准备、真实加载方式、多轮测试和失败修复是发布准备流程的一部分。当前本地维护技能的 `references/testing-lessons.md` 保存可复用经验；具体测试事实仍在项目记录。用户可指定本仓库内的独立子目录，须排除在Git和公开包之外；默认仍用系统临时目录。真实桌面新任务可验证显式路径调用，不强制CLI为唯一宿主；自动发现和对外安装入口另按相应环境核实。候选缺陷修复后只重测受影响场景并独立复核，保留原失败，不要求每次重跑历史案例。

### 当前选择的可安装组合

除非维护者明确改变范围并同步对外说明，本轮发布准备的三个主入口及其完整依赖闭包如下。复制时按目录递归复制，不能只取入口 Markdown。

| 主入口 | 必须随包的目录或文件 | 关键原因 |
| --- | --- | --- |
| `project-startup-standardization` | 该目录的全部 `agents`、`references`、`assets` 与 `SKILL.md` | 入口会按需读取模板和参考 |
| `project-day-closeout` | `project-day-closeout`、`project-directory-closeout`、`project-git-closeout` 三个完整目录 | 前者在 metadata 中声明两个辅助依赖 |
| `bubugao` | 完整 `bubugao` 目录，特别是 `LICENSE.upstream` 和 `references/upstream/` | 上游访谈、术语和 ADR 指令及其许可随包提供 |

根 `LICENSE`、`VERSION`、当前安装更新说明、中英文首页中与上述组合有关的说明，以及一份可审阅的 Release 更新说明，都是发布候选的一部分。根 `VERSION` 必须和目标标签中每个 `skills/*/SKILL.md` 的 `metadata.version` 一致；当前检查器也会核对此约定。

`skill-release-prep` 是维护入口，不属于上述读者安装组合或首页三个主技能；`reader-first-writing` 仍是单独候选。本次 `0.1.0` 预发布候选只选择上述五个技能目录及必要根文件，两者都不在公开包内。GitHub 的 Source code ZIP 会包含标签中的全部文件：在公开标签前，必须明确标签的完整文件范围。若未来未选择的技能仍会随标签公开，要么单独确认它作为公开源码的范围和许可，要么在另有授权的工作中移出目标；不能用“五目录安装组合”掩盖它。

### 本仓库必须留下的发布准备证据

先在冻结候选上运行：

```text
git status --short
git diff --check
python scripts/validate.py
python -m unittest discover -s tests -v
```

静态检查会扫描 `skills/` 下的每个技能，因此通过不表示当前五目录组合已经完整、可加载或获得发布授权。把实际输出、候选版本、精确目录范围和未覆盖项写入已有的项目记录或本次已授权的发布准备产物；不要为了清单另建长期台账。

在上面的基础上，完成以下与当前组合直接有关的事项：

1. **公开化与许可。** 逐项阅读候选目录及会随标签公开的文件，核对作者路径、私人正文、账号或凭据、未随包依赖、固定个人运行环境和第三方来源。根 MIT 许可必须保留；步步高还必须保留上游许可和原始随包参考。关键词扫描和静态检查只能辅助，不能替代内容判断。
2. **真实加载与行为。** 在隔离练习项目中只安装候选包的完整目录，不从维护者机器已有同名技能补依赖。使用真实宿主的新会话或等效干净发现环境，确认三个主入口均被发现，报告实际读取的已安装 `SKILL.md` 路径，并各完成一次明确调用。至少覆盖下表的关键正反行为：

   | 技能 | 正向场景 | 拒绝或保护场景 |
   | --- | --- | --- |
   | 项目建档 | 在获准范围建立或复用最小入口与责任关系 | 明确只读时不写文件，已有规则时不另造冲突规范 |
   | 打完收工 | 依赖齐全且授权明确时，只对本任务稳定文件完成完整收工 | 未知暂存项或缺辅助依赖时不清空、不夹带、不把受影响阶段说成完成 |
   | 步步高 | 实际多轮讨论能给出 Road，并在适用 Context 上作出可见选择 | Context 不适用或归属待确认时不悄悄写入或把讨论越权变成实现 |

3. **整套更新和回退。** 在测试发现目录先检查旧副本的本地修改并保留完整备份，备份放在所有技能发现目录之外。不要覆盖叠加：以完整新目录替换旧组合后，核对活动目录的相对文件集合与候选一致，确认新文件出现且旧残留没有继续被发现。故意缺一个收工辅助依赖，确认安装或更新不会把它称为完成、不会转去读取隐藏的已安装副本；随后恢复完整旧组合并再次核对实际发现路径和调用。用户本地修改若未比较、保留或得到明确处置，不得被新版悄悄丢弃。
4. **远程目标和授权边界。** 在冻结前写明目标托管平台、账号或组织、仓库、可见性、目标分支或候选 ref、版本与标签、更新说明位置，以及当前允许的外部动作。通过平台的实际权限信息确认目标写入权限和分支约束；读到账号信息、存在登录态或本地 Git 成功都不算。目标、权限或分支未知或不可验证时，状态只能是 `blocked`，不能以“本地就绪”替代。
5. **两次独立复核。** 行为和安装测试后，由未参与候选整理或测试执行的独立 Agent 直接阅读行为源码和实际产物，核对边界、依赖、公开化和关键测试结论。随后冻结候选；由独立 Agent 对冻结的准确文件范围、版本、许可、安装更新说明、更新说明和证据做最终复核。第二次不能只复述第一次结论。独立复核不可用时不能自审通过。

修复问题后，只重跑受影响的行为、文件核对或复核；不把无关部分重新排队。任何影响已复核内容的修改会使该范围的复核失效，必须在新内容上做针对性复核。没有关键问题、待确认范围、缺失依赖、未验证真实加载或未完成复核，才可以写“发布准备完成”。

若目标和真实权限均已验证，但维护者尚未授权执行远程动作，可报告“发布准备完成，等待明确发布指令”；这不等于已推送或已创建 Release。实际发布后从 Release 重新下载和试装是发布后验收，不能在发布准备阶段声称已经完成。

## 提交PR

PR是“请维护者把我的修改合进去”的申请，不会自动改变正式发布版。

1. 在GitHub Fork本仓库，在自己的分支修改；较大的行为变化先开Issue讨论。
2. 一次PR聚焦一个问题。提交前查看差异，只选择这次修改的文件。
3. 在PR正文写清：原问题、改变后的行为、验证方法和结果、尚未验证之处；有新来源时说明来源及许可。
4. 维护者检查范围、文档和验证，必要时要求修改，再决定是否合并。尚未完成可开Draft PR。
5. 不在评论中贴真实凭据或私人项目文件；报告问题可以用删去私人内容后的最小例子。

提交贡献表示你有权提交相关内容，并同意这些贡献按本仓库MIT许可分发；保留第三方原有许可要求。已有外部内容不能仅靠改名成为自己的作品。

## 发布一个版本（完成发布准备后）

首版用整个仓库统一版本号。小修复增加最后一位，兼容的新能力增加中间一位；不兼容变化须明确说明迁移方法。0.x阶段也必须披露行为变化，不能因为是早期版本就省略。

当前公开仓库为 [naiman-debug/naiman-skills](https://github.com/naiman-debug/naiman-skills)，实际版本入口为 [Releases](https://github.com/naiman-debug/naiman-skills/releases)。`RELEASE_NOTES.md` 可以作为候选更新说明；只有版本页出现对应 Release 后，才可把它写成实际发布事实。

1. 只使用已冻结且被最终复核的候选。若版本、文件、许可、安装说明或更新说明发生变化，先恢复受影响范围的验证与复核；不要把修改后的内容放进旧结论。
2. 检查将进入 Git 的准确文件和署名来源，只暂存本次公开内容。在仓库拥有者明确授权后，推送已经确认有写入权限的指定目标分支与版本标签；不要推送整个个人技能目录或其他仓库历史。
3. 在 GitHub 给这个确定提交创建 `v版本号` 标签和 Release。Release 说明写清变化、涉及技能、完整依赖组合、安装更新方法、已验证环境和已知限制；根 LICENSE 必须随发布包保留。测试尚不完整时不要称为稳定版；首版可采用明确标注限制的预发布，但不能跳过关键安装及权限行为验证。
4. 从实际 Release 重新下载，在独立目录核对并试装；成功后再将 README 和项目记录更新为实际发布状态。不要把“已推送”写成“用户已安装”。这是发布后验收，不是发布前已经取得的证据。

标签固定一个版本的内容，不移动旧标签偷偷替换发布内容。后续修复用新版本，旧用户仍可选择原版。Release说明是面向用户的更新记录，不要求每个技能再维护一份同内容日志。

GitHub提供标签对应的ZIP下载。官方参考：[Releases](https://docs.github.com/en/repositories/releasing-projects-on-github/about-releases)、[PR](https://docs.github.com/en/pull-requests/reference/pull-requests)。

## Maintainers and English-speaking contributors

Keep a PR focused on one concrete change. Describe the problem, new behavior, tests and known limits. Add only content you may distribute under MIT. Update skill instructions and user-facing docs together. Run the validator and its tests; behavior changes also need a scoped practice-project run. No push or release is implied by a local edit. The detailed maintenance guide is currently Chinese; English PR descriptions are welcome.
