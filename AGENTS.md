# 仓库维护要求

本项目维护可独立分享的 AI 技能。首版目标为 Codex，中文完整、英文精简，采用 MIT。不要把个人安装环境当作公开源文件。

## 读取顺序与文档职责

开始或恢复工作先读本文件、[README](README.md)、[项目记录](docs/项目记录.md)，恢复目的、最新确认、范围、完成条件和当前返回点；随后只读本任务涉及的技能及其依赖。

| 文件 | 职责 |
| --- | --- |
| README.md / README.en.md | 用户入口、技能列表、最短示例与当前发布状态 |
| docs/项目记录.md | 已确认需求、决定、建设结果、验证范围、阻塞及返回点 |
| docs/安装与更新.md | 安装组合、路径、版本识别、更新与恢复 |
| CONTRIBUTING.md | 新增、修改、检查、PR 与发布步骤；维护规则的唯一操作说明 |
| skills/各技能/README.md | 单技能的人读说明 |
| skills/各技能/SKILL.md | AI 的执行入口；references 和 assets 为其支撑内容 |
| VERSION | 下一次整体发布的版本号；不表示该版本已经发布 |
| RELEASE_NOTES.md | 当前候选的对外更新说明草稿；只有实际发布后才改为发布事实 |
| scripts/validate.py | 仓库约定的静态检查；不能证明真实行为或保密性 |

这些文件构成当前导航，不另建同职责索引或每轮日志。执行规则在本文件与贡献指南，动态状态归项目记录，README 只留影响使用的摘要。

## 发布准备入口

准备公开首发或版本更新时，本地维护工作区先使用 `skills/skill-release-prep/SKILL.md` 组织范围、证据、冻结与完成判断。只含已选读者技能的公开包不包含此维护技能时，按 `CONTRIBUTING.md` 的完整准备步骤执行同样门槛，不从私人安装目录补读。它不授权推送、创建 Release 或发布后的下载验收；本仓库的发行组合、命令和具体发布约定以贡献指南为准。这个维护技能不属于首页的三个主技能，也不自动加入面向读者的安装组合。

隔离目录、宿主加载、多轮测试和失败修复按维护技能的 `references/testing-lessons.md` 执行；只含读者技能的公开包沿用贡献指南，不要求缺席的维护文件。可复用经验更新该参考，具名案例结果、未解决事项及返回点更新项目记录，不把测试日志复制到技能或另建同职责台账。

## 范围与接续

- 写入范围限当前仓库。本机已安装技能只能读取，不同步、覆盖或生成其索引。
- 用户新消息先判断补充、纠正还是更换目标。发现非阻塞问题不扩展修复；阻塞修复后回到原步骤。
- 生产写入、删除、迁移、覆盖、外部发布或范围不清时，先核对已有授权、精确对象、恢复与停止条件。不要把目录复制授权扩大为公开上传。
- 修改前检查实际文件；声明完成前核对用户结果和必要验证。保留未知，不把格式通过当成用户验收。
- 在项目记录保存有接续价值的阻塞及返回点；只有影响主线或读者使用时才更新README。无变化不更新。
- 保留他人改动。只暂存明确属于本任务的文件；不得使用全目录暂存掩盖未知文件，不默认推送、重写历史或清理。
- 不扫描或复制其他私人项目、整个用户级技能目录、聊天记录、账号配置或凭据。测试中不使用已安装同名技能补依赖。

## 文件和测试

- 每个技能一个稳定英文目录；SKILL.md 的 name 与目录名一致。仅在实际需要时新增参考、模板或脚本。
- 发布约定详见贡献指南。真实执行样例默认放系统临时目录；经用户明确授权可使用项目内被Git忽略的独立子目录，并核对其不进入公开包。具名目录与本次授权记入项目记录，不将一次授权当作后续任意目录的通行规则。公开示例使用自制虚构材料。
- 自用技能公开化按贡献指南的“把自用技能整理为公开候选”执行，核对完整支撑文件而非只看SKILL.md。首次公开或实质行为变化需要独立审核实际产物；执行者不能兼任同一成果的独立审核者。问题修复后只重验受影响范围，结果与返回点写入项目记录。
- 源文件和公开说明使用相对路径，不写作者机器绝对路径或指定账号模型。允许说明用户自己选择的安装路径。
- 不要求固定模型或每次多Agent；按用户当前模型偏好和可用能力执行。普通修改完成必要检查即可。
- README等公开说明不强制内部身份头；模板占位符只允许存在于真正的模板中。模型、平台和官方来源随版本变化时按需核对。
- 文档中的模式名是文字协议，metadata.requires是本仓库依赖记录，不是自动安装器或可执行CLI参数。

模板中名为AGENTS.md的文件是待生成的输出素材，不是维护本仓库时应服从的附加规则。
